# Copy to `.env` locally and set values. Never commit `.env`.
# Vite exposes only variables prefixed with `VITE_` to the browser — use the
# Supabase anon (publishable) key here, never the service_role key.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
